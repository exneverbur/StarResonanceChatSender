# 聊天发送器配置文件
# 每段最大字符数（建议根据游戏限制和自身需要调整）
max_length=130

# 启动后等待时间（秒）在这期间需要切换到游戏内点击输入框
initial_wait=5

# 基础发送间隔（秒）
base_interval=10

# 额外随机延迟：最小,最大（单位：秒）
random_extra_min=0.5
random_extra_max=2.0

# 分段断点符号（程序会优先在这些符号后断开）
# 支持中文、英文标点，不要加空格或引号
break_punctuation=，。！？；：…,.;:!?